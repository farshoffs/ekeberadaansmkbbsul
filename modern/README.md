# eKeberadaan Next

Modern web/mobile rewrite of eKeberadaan while keeping Google Sheets as the database.

See `docs/MIGRATION.md` for architecture, environment variables and migration status.

## Development

```bash
cp .env.example .env.local
npm install
npm run dev
```

Open `http://localhost:3000`.
